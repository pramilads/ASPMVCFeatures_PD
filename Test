import { SchedulerService } from 'src/app/core/services/scheduler.service';
import { Component, EventEmitter, Input, OnInit, Output, SimpleChanges } from '@angular/core';
import {
  NgbModal,
  NgbModalOptions,
  ModalDismissReasons,
} from '@ng-bootstrap/ng-bootstrap';

import { EmployeeEditComponent } from '../employee-edit/employee-edit.component';
import { SortEvent, compare } from 'src/app/shared/directives/sort.directive';

@Component({
  selector: 'app-crew-info',
  templateUrl: 'crew-info.component.html',
})
export class CrewInfoComponent implements OnInit {
  @Input() crewList: any = [];
  sortedEmployeeList: any;
  @Output() outputEmployeeList = new EventEmitter<any>();
  currentPage = 1;
  recordsPerPage: number;
  maxPageToDisplay: number;
  columnsToSort: any = {};
  formCrewDisabled: boolean = true;

  constructor(private modalService: NgbModal, private schedulerService: SchedulerService) {}

  ngOnInit(): void {
    this.sortedEmployeeList = this.crewList;
    this.recordsPerPage = this.crewList?.length;
    this.maxPageToDisplay = Math.ceil(
      this.crewList?.length / this.recordsPerPage
    );
  }

  ngOnChanges(changes: SimpleChanges){
    this.sortedEmployeeList= changes.crewList?.currentValue;
    this.recordsPerPage = changes.crewList?.currentValue?.length;
    this.maxPageToDisplay = Math.ceil(
      changes.crewList?.currentValue?.length / this.recordsPerPage
    );
  }

  setPageSize(evt): void {
    this.recordsPerPage =
      evt.target.value === '' ? this.crewList?.length : evt.target?.value;
  }

  editEmployee(empId): void {
    const modalOptions: NgbModalOptions = {
      size: 'lg',
      keyboard: false,
      backdrop: 'static',
    };
    const modalRef = this.modalService.open(
      EmployeeEditComponent,
      modalOptions
    );
    const empInfo = JSON.parse(JSON.stringify(this.crewList)); // get a copy of employee list
    modalRef.componentInstance.inputEmployeeInfo = empInfo.filter(
      (emp) => emp.id === empId
    )[0];

    modalRef.result.then(
      (resolveReason) => {
        if (resolveReason === 'save') {
          this.outputEmployeeList.emit();
        } else if (resolveReason === 'cancel') {
          alert('do nothing');
        }
      },
      (rejectReason) => {
        alert(`Dismissed ${this.getDismissReason(rejectReason)}`);
      }
    );
  }

  private getDismissReason(reason: any): string {
    if (reason === ModalDismissReasons.ESC) {
      return 'by pressing ESC';
    } else if (reason === ModalDismissReasons.BACKDROP_CLICK) {
      return 'by clicking on a backdrop';
    } else {
      return `with: ${reason}`;
    }
  }

  onSort({ column, direction }: SortEvent): void {
    this.columnsToSort = { ...this.columnsToSort, [column]: direction };

    // sorting
    const noOfSortedCols = Object.keys(this.columnsToSort).filter(
      (key) => this.columnsToSort[key] !== ''
    ).length;
    if (noOfSortedCols === 0) {
      this.sortedEmployeeList = this.crewList;
    } else {
      this.sortedEmployeeList = [...this.crewList].sort((a, b) => {
        return compare(a, b, this.columnsToSort);
      });
    }
  }

  setSelectedCrewMembers(evt, emp){
    this.schedulerService.selectedCrewMembers.push(emp);
    emp['isSelected'] = evt.target.checked;
    this.formCrewDisabled = false;
  }

  formCrew() {
    this.schedulerService.formCrewSubject.next();
    this.sortedEmployeeList.forEach(emp => emp.isSelected = false);
    this.formCrewDisabled = true;
  }
}
