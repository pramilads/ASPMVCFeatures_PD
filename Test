<div aria-labelledby="tab-workforce" id="tab-workforce-content" class="tab-pane fade active show" role="tabpanel"
    [hidden]="!schedulerService.expandSearch">

    <div class="row">
        <div class="col-12 pt-3">
            <div>
                <label class="col-form-label">Filter Records: </label>
                <mat-form-field *ngFor="let filter of filterSelectObj; let i = index" style="margin-left: 
    15px;width: 100px;">
                    <mat-label>{{filter.name}}</mat-label>
                    <mat-select #matSelect multiple [formControl]="filter.formCtrl" [(ngModel)]="filter.preSelected">
                        <mat-option *ngFor="let item of filter.options" [value]="item.id">{{item.name}}</mat-option>
                        <button class="btn btn-primary btn-sm m-2" (click)="ok(i, filter)">Ok</button>
                        <button class="btn btn-primary btn-sm" (click)="cancel(i, filter)">Cancel</button>
                    </mat-select>
                </mat-form-field>
                &nbsp;
                <div style="display:inline-block">
                    <button type="reset" class="btn btn-primary btn-sm" (click)="resetSearchData()">Reset</button>
                </div>
            </div>
        </div>
    </div>

    <div class="row mt-3">
        <div class="col-12">
            <app-crew-info *ngIf="displayEmployeeList" [crewList]="filteredCrewInfo"
                (outputEmployeeList)="reloadEmployee()">
            </app-crew-info>
        </div>
    </div>
